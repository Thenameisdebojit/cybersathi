# backend/rasa/credentials.yml
# Used by Rasa to connect to chat channels when running 'rasa run'
rest:
  # Use REST channel during development
  #
  # Example: curl -XPOST http://localhost:5005/webhooks/rest/webhook -d '{"sender":"user1","message":"hi"}'
  #

socketio:
  # configure if using socket.io

# Note: For WhatsApp, we will use the backend adapter to receive WhatsApp webhooks
# from Meta Cloud / Twilio and forward them to Rasa via the REST channel or nlu endpoint.
